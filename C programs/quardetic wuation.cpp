#include<stdio.h>
int main(){
	 int d,a,b,c,root1,root2,t;
	 printf("Enter the a , b and c");
	 scanf("%d%d%d",&a,&b,&c);
	 d=sqrt(b*b-4*a*c);
	 t=-b/2a
	 if(d>0){
	 	root1=(-b+d)/2*a;
	 	root2=(-b-d)/2*a;
	 }
	 else if(d==0){
	 	root1=t;
	 }
	 else 
	 root1=t
}
